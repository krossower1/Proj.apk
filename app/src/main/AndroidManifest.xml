<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.spacer"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- Uprawnienia -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- UWAGA: to uprawnienie było błędne i usunięte -->
    <!-- <uses-permission android:name="android.permission.android:resource" /> -->
    <!-- <uses-permission android:name="android.permission.MediaStore.createWriteRequest" /> -->
    <!-- te dwie linie nie są potrzebne i powodowały błędy -->



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icons8boots50"
        android:label="StepTracker"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Niepotrzebne meta-data – usunięto niepoprawny atrybut -->
        <!-- <meta-data ... /> -->
        <!-- SplashActivity jako launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:label="StepTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Główna aktywność uruchamiana przy starcie -->
        <!-- <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="StepTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->

        <!-- LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="StepTracker" />

        <!-- Ekran Spacer (jeśli istnieje w projekcie) -->
        <activity
            android:name=".Spacer"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:label="@string/title_activity_spacer" />

        <activity
            android:name=".RegisterActivity"
            android:exported="true"
            android:label="Rejestracja" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Główna" />

        <activity android:name=".SettingsActivity" />
        <activity android:name=".AccountActivity" />
    </application>
</manifest>
